<html lang="en">
<head>
  <style type="text/css">
    body {
      font-family: Arial, Helvetica, sans-serif;
      background-color: #e8e8e8;
      font-size: 11pt;
    }

    .content {
      margin-top: 100px;
      display: flex;
    }

    .spacing {
      flex: 1;
    }

    .box {
      background-color: white;
      padding-left: 20px;
      padding-right: 20px;
      padding-bottom: 20px;

      -webkit-box-shadow: 0px 0px 10px 1px rgba(0,0,0,0.5);
      -moz-box-shadow: 0px 0px 10px 1px rgba(0,0,0,0.5);
      box-shadow: 0px 0px 10px 1px rgba(0,0,0,0.5);
    }

    .headline {
      margin-top: 15px;
      margin-left: -20px;
      margin-right: -20px;
      padding: 10px 20px;
      background-color: #660000;
    }

    h1 {
      color: white;
      font-size: 12pt;
      margin: 0;
    }

    .path {
      color: #606060;
      font-size: small;
      margin: 0;
      padding: 0;
      margin-bottom: 2px;
    }

    .message {
      margin: 0;
      padding: 0;
      margin-bottom: 10px;
    }
  </style>
  <title>Bad Request</title>
</head>
<body>
<div class="content">
  <div class="spacing"></div>
  <div class="box">
    <div class="headline">
      <h1>Invalid Parameters</h1>
    </div>
    <p>The request was invalid because the validation of the parameters has failed for the following reasons:</p>
    <% result.errors.each do |error| %>
      <p class="path"><%= error[:path].join(' / ') %></p>
      <p class="message"><%= error[:message] %></p>
    <% end %>
  </div>
  <div class="spacing"></div>
</div>
</body>
</html>